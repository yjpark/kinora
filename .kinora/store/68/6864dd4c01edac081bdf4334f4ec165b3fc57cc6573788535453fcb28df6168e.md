# Task: Adopt the jig rust lint-rules framework

**Goal.** Bring kinora's own repo onto the jig lint-rules framework so coding
rules are enforced by the toolchain, not by agent memory. Import the three
rung-1 rust rules as kinos in a new `rules` root and wire their mechanisms.

**Realizes rules:**
- kino://bdb3ccd82d5e0c445f71673431b90440079c9eefe90f19d80396e5d999025eea (jig::rust::no-unwrap-in-lib)
- kino://c65334d02366f3f1de710a13e5c5ea3376728b0520173d314315e998b3d28fb3 (jig::rust::no-todo-committed)
- kino://ed2cb00f8eb9d695a32f254c55ae4722e8b2f30a9b782d3b6911e00d61f046a9 (jig::rust::prefer-file-modules)

**Done when:** the `rules` root is declared, the three rule kinos resolve, the
clippy lints + arch-test mechanisms are wired and cite their rule with `# rule:`
/ `implements:`, the whole workspace is clippy-clean under the new lints, and
`tests/architecture.rs` passes.

## Outcome
- `.kinora/config.styx`: added `rules { policy "never" }`.
- Three rule kinos stored with `--provenance jig` and `rule::*` ladder metadata
  (all rung 1, target-rung 1; clippy for the two idiom rules, arch-test for the
  structure rule), per `templates/rust/rules/manifest.toml`.
- `Cargo.toml` `[workspace.lints.clippy]`: added `unwrap_used`, `expect_used`
  (→ no-unwrap-in-lib), `todo`, `unimplemented`, `dbg_macro` (→ no-todo-committed),
  each carrying its `# rule:` back-ref. `clippy.toml` carves out unwrap/expect/dbg
  in tests.
- `crates/kinora/tests/architecture.rs`: the arch-test mechanism, adapted for a
  virtual workspace — walks every `crates/*/src` from the workspace root and
  asserts no `mod.rs`. Cites `implements: jig::rust::prefer-file-modules`.
- Four pre-existing lib violations fixed by refactor (not `#[allow]`):
  `kinora/src/store.rs` (parent-dir → real error), `kinora/src/resolve.rs`
  (`get_mut` → `else continue`), two guarded `unwrap`s in `stencil/src/region.rs`
  (→ `if let`).
- CLAUDE.md: `<!-- jig:rust -->` section gained the "Lint Rules" subsection.
- Workspace: 0 clippy warnings, all tests pass incl. the new arch test.
