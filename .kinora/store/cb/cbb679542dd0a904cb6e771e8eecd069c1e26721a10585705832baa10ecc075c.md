# Task: Dogfood — migrate kudo RFCs as the first kinos

**Goal.** Prove the bootstrap loop by taking real content into `.kinora/store/` via `store` and rendering it cleanly — closing the bootstrap epic and unblocking RFC-0003 being marked done in kudo.

**Realizes:**
- [[kinora-w7w0:spec]] — Kinora bootstrap (this is the dogfood step).
- [[kinora-860i:task]] — `store`.
- [[kinora-6zxd:task]] — `resolve`.
- [[kinora-zboo:task]] — kinograph composition.
- [[kinora-9nom:task]] — `render`.

**Done when:** real content is stored as kinos with provenance recorded and `render` produces a readable mdbook site — verified end-to-end with `mdbook build`. (Settled: the project's design principles were stored into this repo's `.kinora/`; the kudo RFCs are kept as RFCs rather than imported, so the cross-repo RFC import and multi-RFC kinograph were not pursued.)

## Outcome
- The store → render loop was exercised end-to-end: RFC-0003 imported as a kino (id `e55f4908…`, lineage `7a155b58`) and `render` produced a readable mdbook site, verified via `mdbook build`.
- **Scope landed on storing this repo's design principles into `.kinora/` rather than importing the kudo RFCs** — the RFCs stay as RFCs. Consequently the multi-RFC `kudo-rfcs` kinograph was dropped (a one-entry kinograph adds no value), and RFC-0001/0002 import was not pursued.
- The original cross-repo import was flagged as needing supervised (non-night-shift) execution because it writes to the kudo repo; that path was ultimately not taken.
