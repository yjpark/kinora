# Task: Dogfood — capture design principles as kinos

**Goal.** Break the design principles so far (store layout, hot ledger + branch
handling, event envelope, kinograph composition, etc.) into individual focused kinos
stored in this repo, composed into a kinograph so `render` produces a readable
principles site — exercising the public `kinora store` CLI end to end.

**Done when:** each design decision is a stored `kind=markdown` kino tagged
`kind=design-principle`, composed into a `design-principles` kinograph that resolves
all members and renders (via mdbook) successfully.

## Outcome
- Authored 11 design-principle kinos and composed them into a `design-principles`
  kinograph, all via `kinora store` (markdown + kinograph kinds): content-addressed
  store, hot ledger one-file-per-event, event envelope, identity-and-versions,
  kinograph-composition-by-reference, name-vs-id, provenance-mandatory,
  metadata-namespaces, branch-merge-safety, append-only-nothing-mutates,
  future-root-kinograph-chains.
- Verification: `resolve design-principles` returns the composed listing with all 11
  ids; `render --cache-dir` produces 13 pages; `mdbook build` succeeds.
- Dogfood findings spun off as follow-ups: render branch-label derivation under the
  hot-ledger layout ([[kinora-ml4t:task]]) and the misleading `lineage=` store
  wording ([[kinora-6395:task]]).
