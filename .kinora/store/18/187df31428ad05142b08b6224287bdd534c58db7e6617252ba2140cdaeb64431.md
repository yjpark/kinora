# Task: record head_ts on RootEntry so GC is independent of staging

**Goal.** Record the head store event's ts directly on `RootEntry` at commit time and have MaxAge GC read it from the entry instead of looking it up in the staged event stream — decoupling retention semantics from staging.

**Realizes:**
- kino://50d1260aba15f848bb3656667a42f08ebcff7e3bcf3fb1d815f24ae6399ddd24 — Multiple named roots, each with its own GC policy (GC retention semantics).
- kino://e412ee1431b0dd04de510c89207e309538001c8fb69fa62686b672d52ef168cd — GC/prune per policy + pin support (extends the entry-GC path).

**Done when:** `RootEntry` gains `head_ts: String` (with `#[facet(default)]` so legacy blobs parse with an empty ts); `build_root` populates it from the picked head event; `apply_root_entry_gc` reads `entry.head_ts` (empty → keep, conservative); and existing MaxAge/KeepLastN/Never tests still pass.

## Outcome
Resolved. Added `head_ts` to `RootEntry`; `apply_root_entry_gc` dropped its `events` arg and now reads the entry's ts (empty → keep, parse error → log + keep, older than cutoff → drop unless pinned). `propagate_pins` copies `head_ts` alongside `version` so pinned entries don't carry a mismatched ts. `resolve.rs::ingest_root_kinographs` passes `re.head_ts` into the synthesized store Event so render/resolve see a real ts for archive-drained heads. 6 new tests; 509 workspace tests pass. Unblocks kinora-wcpp (MaxAge staged events can be drained after archive without orphaning entries from their retention signal). Commits `c17e767`, `c4fb25f`, `0c53fac`.
