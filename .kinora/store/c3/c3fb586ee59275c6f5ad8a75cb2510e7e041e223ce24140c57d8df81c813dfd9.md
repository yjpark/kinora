# Task: CLI — stencil sync <paths>

**Goal.** Implement `stencil sync <paths>`: scan files/dirs (recursive; default
cwd) for stencil markers, apply the engine, write changed files atomically, and
report files changed / slots filled / drift warnings / unslotted entries.
Non-zero exit on errors. TDD/integration.

**Realizes:**
- [[kinora-bm7z:spec]] — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Done when:** `run_sync` loads the Resolver, collects files (file verbatim, dir
walked for `*.rs` skipping `target/`/dot-dirs, canonical dedup), syncs each via
`sync_file`, atomic-writes changed files, and renders a summary; repo-level
failures are fatal while per-file failures are collected;
`SyncSummary::has_errors()` (per-file error or unmatched slot) drives a non-zero
exit; all tests pass with zero warnings.

## Outcome
Implemented `crates/stencil-cli/src/sync.rs`: `run_sync`, `collect_files` +
`walk_dir` (canonical-identity dedup, symlinks not followed), `atomic_write`
(tmp sibling + rename), `format_sync_summary`. Re-added the `kinora` dep +
`tempfile` dev-dep; added repo-root resolution and Io/Resolve/NotInKinoraRepo/
PathNotFound to `common.rs`. 19 unit tests; full workspace green, zero warnings,
clippy clean. Review fixes: orphan blocks surfaced, canonical-keyed dedup,
documented symlink skip.
