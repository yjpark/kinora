# Plan: `kinora repack` — commit + clone-to-temp + atomic swap

**Task:** kino://c6636ca3154762f356cf3b185d2c5ff84749a69792614c65ee2608bec878cd3f

Library pipeline first, then the CLI wrapper, then review fixes.

## Phase A — library `kinora::repack::repack_repo` *(done)*
Preflight (refuse on lingering siblings), run `commit_all` (refuse swap if any
root failed), `clone_repo` into `.kinora.repack-tmp`, two-rename swap with rollback
on rename-2 failure, unlink `.kinora.repack-old`.

> **Done.** Implemented with the two-rename critical section + rollback. 6 tests:
> empty-repo success, preflight errors for lingering `.repack-tmp`/`.repack-old`,
> pending staged committed before swap, no lingering siblings after success, legacy
> extensionless filename rewrite through clone.

## Phase B — CLI command `kinora repack` *(done)*
Normal `find_repo_root` walk-up; global `-C` handled in main.rs before dispatch;
format the pass-through clone summary.

> **Done.** 7 CLI tests: empty-repo success, outside-kinora-repo error,
> author-unresolved error, provenance default, summary rendering at zero/one/multi
> counts with no-op lines filtered.

## Phase C — review fixes *(done)*
> **Done.** Folded into the CLI commit (`e5c0ad8`) — a doc-comment correction on
> the `.repack-old` cleanup. Git-dirty rail and rename-2 rollback test were
> consciously deferred (see kino://f1a123d9f07b03c46af0b372fba2303a21889c57ccb6450e9e202292ba6b6420).
