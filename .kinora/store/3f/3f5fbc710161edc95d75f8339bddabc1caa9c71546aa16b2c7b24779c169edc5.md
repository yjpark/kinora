# Task: Extract content-hash + content-store crates

**Goal.** Carry out the staged extraction of the content-addressing core into the
`content-hash` and `content-store` crates per the design contract.

**Realizes:**
- [[kinora-kn71:spec]] — Extract content-addressing into standalone crates.

**Done when:** both crates compile standalone with their own tests, kinora depends on
them (re-exporting `Hash` and `ContentStore` for source compatibility), `write(kind)`
is replaced by `write(content, ext)` with `ext_for_kind` staying in kinora, the
`BlobBackend`/`LooseFileBackend` seam is in place, the full workspace is green with
zero behavioral change (hash-preserving), and each migration stage is its own commit.
