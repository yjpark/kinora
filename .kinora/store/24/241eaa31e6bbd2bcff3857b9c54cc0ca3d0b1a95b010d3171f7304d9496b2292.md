# Task: Clean staged after commit; preserve history as `commits`-root archive

**Goal.** Implement the per-commit archive lifecycle: after a successful commit,
serialize the consumed staged events into a `commit-archive` kino under the
reserved `commits` root, and exclude that root from default render.

**Realizes:**
- [[kinora-q6bo:spec]] — Commit archives staged events into a per-root `commit-archive` kino.

**Plan:** [[kinora-q6bo:plan]]

**Done when:** after `kinora commit`, an archive kino + assign into `commits` is
produced for each non-commits root that promoted work; the `commits` root never
archives itself; the commits kinograph contains the archive; repeated runs with
unchanged state are idempotent; default render ignores the `commits` root; zero
warnings.

## Outcome
Shipped across 5 commits. `commits` root auto-provisioned with `RootPolicy::Never`;
new `kinora::commit_archive` module (v1 JSONL header + one event-json per line);
`commit_all` reordered so `commits` iterates last; `maybe_archive_owned_events`
wired into `commit_root_with_refs`; `commit-archive` added to RESERVED_KINDS with a
`jsonl` extension and skipped in render. Staged cleanup itself was deferred to
[[kinora-bayr:task]] (needs the prior-root merge so `build_root` still sees
archived-and-pruned kinos across subsequent commits).
