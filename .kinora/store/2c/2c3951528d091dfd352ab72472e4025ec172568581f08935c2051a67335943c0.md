# Task: Resolver materializes kinos from pruned Never-policy roots

**Goal.** Extend `Resolver::load` to ingest committed root kinographs so
resolve/render still find kinos whose staged store events were pruned by the
Never-policy drain.

**Realizes:**
- kino://7f01e394125462293dd11ccba0259cd3b95b9a8dc72742cd01d969e752e8ea2c — Prune staged events after archive, rebuild via prior-root merge.

**Done when:** `resolve_by_id` works after a Never commit with store events
pruned; render groups Never-committed kinos under their root; commit→resolve→render
succeeds end-to-end; the bayr Never→keep-last-10 render workaround is reverted;
zero warnings; full suite green.

## Outcome
Bugfix shipped in 3 commits (503 tests). `Resolver::load` now has a third pass:
it walks committed root kinographs and synthesizes a store event for any
(id, version) pair not already represented, pulling kind/metadata from the
kinograph entry — content reads still work because the pinned version hash IS the
ContentStore key. Reverted bayr's render-test workaround back to Never. The
commit.rs (pin/cross-root) and clone.rs (assign-event) workarounds stay — neither
is bwo8-relevant. Review caught one real issue (dedup set must be mutable across
kinograph passes), fixed in the third commit.
