# Task: assign event + `kinora assign` CLI + `store --root` atomic pair

**Goal.** Land the user-visible surface for kino ownership: the `assign` event type, the `kinora assign` CLI, and the `kinora store --root <name>` birth-assign atomic pair. (Compact does not yet consume assigns.)

**Realizes:**
- kino://344d509b8e3e8a39cd1bf7f2846f383e976bf5821764a2223503aa0e9431e32e — Root ownership is exclusive and structural; kinos move via assign events.

**Plan:** kino://c2310898765fc48dbed39b54355716949ffa2592a18f48d4aa28740b7ca4f0f9

**Done when:** `AssignEvent` (with possibly-empty `supersedes`) round-trips through the hot ledger one-file-per-event path with the same hashing discipline as store events; `kinora assign <kino-or-name> <root> --resolves <hashes>` resolves the kino and populates `supersedes`; `kinora store --root <name>` writes the birth event + an `assign → name` event as an atomic pair (rollback on failure, no orphaned hot file); and plain `kinora store` still writes exactly one event.

## Outcome
Resolved. Library `assign.rs`: `AssignEvent` mapped onto the flat `Event` (`event_kind = "assign"`, `parents = supersedes`, `kin::target_root` metadata), `write_assign` with full validation, `AssignError`. CLI: `kinora assign` runner (id-first then name resolution, git-author fallback) + `store --root` with `pair_assign_with_rollback` (best-effort deletes the store hot file on assign failure; content blob intentionally not rolled back — dedup-safe). Root existence and superseded-hash existence are deliberately compact-time checks, not assign-time. 24 lib + 16 CLI tests. Commits `2fa276a`, `b7cd5fe`, `9a21efc`, `c50ac54`, `235148a`.
