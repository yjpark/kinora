# Task: `kinora clone` — rebuild `.kinora/` into a fresh directory

**Goal.** Build the `kinora::clone` library module (reachability walk → re-store →
rebuild root kinographs → empty staged) and a thin `kinora clone <src> <dst>` CLI
that takes direct `.kinora/` paths.

**Realizes:**
- [[kinora-b1mg:spec]] — `kinora clone` rebuilds `.kinora/` into a fresh directory.
- [[kinora-q6bo:spec]] — Commit archives staged events into a per-root `commit-archive` kino.

**Plan:** [[kinora-b1mg:plan]]

**Done when:** `kinora clone .kinora .kinora2` produces a dst that render/resolve
treat identically to src, with zero extensionless blobs, unreachable blobs
dropped, src unchanged, clean error on dst-not-empty and on src hash mismatch;
zero warnings.

## Outcome
Landed in 4 commits. `kinora::clone::clone_repo` walks reachable blobs from each
root's heads (recursing into composition entries), re-stores them through the
current store API, rebuilds root pointers, and filters staged events (keep store
events with reachable hashes, keep assigns verbatim). CLI wrapper rejects
`-C`/`--repo-root`; author/provenance default to `"clone"`. 13 library + 9 CLI
tests. Spec deviation: the staged-empty precondition was dropped pending
[[kinora-bayr:task]] (see plan Phase A).
