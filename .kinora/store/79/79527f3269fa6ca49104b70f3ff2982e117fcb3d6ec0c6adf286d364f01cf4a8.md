# Task: Stencil crate scaffolding + workspace wiring

**Goal.** Create `crates/stencil` (lib) and `crates/stencil-cli` (bin
`stencil`), add to workspace members, depend on the kinora crate, wire logging
and errors to match kinora conventions, and document the `kudo::api-spec` /
`kudo::api-kinograph` kind conventions. No kinora-crate changes.

**Realizes:**
- [[kinora-bm7z:spec]] — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Done when:** both crates are in `members`; `stencil --help` renders; the
`kinds` module exposes `API_SPEC` / `API_KINOGRAPH` and tests confirm they pass
kinora's namespace validation; logging (logforth+fastrace) and errors
(thiserror in lib, rootcause in cli) mirror kinora-cli; all tests pass with zero
warnings.

## Outcome
Scaffolded `crates/stencil` (lib, depends on `kinora`: crate docs, `kinds`
module with `API_SPEC`/`API_KINOGRAPH`, `StencilError` with `From` conversions
for kinora's io/`ResolveError`/`KinographError`) and `crates/stencil-cli` (bin
`stencil`: logforth+fastrace `STENCIL_TRACE=1` reporter, figue `Cli` with
`-C/--repo-root` and a `Command` enum declaring `sync`/`scaffold`, `CliError`
via rootcause; command engines stubbed `NotImplemented` pending the engine/CLI
beans). Both added to workspace `members`; no kinora-crate changes. All tests
pass, zero warnings.
