# Task: Marker protocol + region parser + LanguageTarget trait

**Goal.** Implement the line-based marker parser, the file region model, and the
`LanguageTarget` trait (with `RustTarget`) in the stencil lib, TDD.

**Realizes:**
- kino://4fe343db0e11af818156ab10c05f32841a1aa10078d60ac8973ecdabd5a2dada — Stencil marker protocol, region model, and LanguageTarget trait.

**Done when:** `stencil:kinograph` / `stencil:slot` / `stencil:ro`…`stencil:end`
parse into a `StencilFile` of ordered blocks; `parse → to_source` round-trips
stencil-written input byte-for-byte; `RustTarget` emits `//`/`///` correctly;
error cases (unterminated / unexpected-end / nested-ro / malformed) carry 1-based
line numbers; `ParseError` is wired into `StencilError`; all tests pass with zero
warnings.

## Outcome
Added `target.rs` (`LanguageTarget` + `RustTarget`) and `region.rs` (`Block`:
Text/Binding/Slot/ReadOnly + `StencilFile`, line parser capturing per-marker
indentation, canonical `to_source`). 35 tests (round-trip incl. empty / blank /
trailing-newline / indented / mixed files, all error paths, doc formatting,
deliberate marker normalization). Zero warnings; fresh-perspective review found
no correctness issues.
