# Task: Dogfood — define stencil's own public API as a kinograph and rebuild source

**Goal.** Author `kudo::api-spec` kinos for stencil's public surface, compose a
`kudo::api-kinograph`, place `stencil:kinograph` + `stencil:slot` markers in
stencil's source, run `stencil sync`, and verify the RFC day-one dogfood loop
end-to-end. Scoped to `target.rs` (the smallest self-contained module);
remaining modules tracked in kino://f86205dc24cea33923ee29750126a8cc51afec233dcc0c20176d9b83ef241ea3.

**Realizes:**
- kino://5898fe2453ef8824f71670df106512d00502edbc90ac0fa8705a5d03e6f98c25 — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Done when:** the read-only blocks reproduce the trait + struct from the kinos,
the crate compiles, all stencil tests pass, and a second `stencil sync` reports
0 changed (idempotent no-op) — using the real `kinora` + `stencil` CLIs against
this repo's `.kinora` store.

## Outcome
Proved the RFC-0004 day-one dogfood loop on real stencil source. Authored two
`kudo::api-spec` kinos (`target-language-target`, `target-rust-target`),
composed `stencil-target-api`, restructured `target.rs` to the slot model, and
`stencil sync` created 2 read-only blocks reproducing the items. Crate compiles;
72 stencil tests + full workspace green; second sync 0 changed; rustfmt-neutral.
**Convention established:** one api-spec kino per public item; whole-item
contracts render the full definition; functions split doc+signature from the
editable `{ body }`. Dogfood kinos live as staged ledger events.
