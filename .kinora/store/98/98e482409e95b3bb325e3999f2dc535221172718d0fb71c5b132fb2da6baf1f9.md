# Task: Phase 3 — multiple named roots + assign + per-root GC

**Goal.** Generalize phase-2B's single flat `main` root into multiple named roots, introduce the `assign` event as the mechanism for moving a kino between roots, and enforce per-root GC with pin and cross-root integrity.

**Realizes:**
- [[kinora-xi21:spec-4]] — Multiple named roots, each with its own GC policy; inbox is the default.
- [[kinora-xi21:spec-5]] — Root ownership is exclusive and structural; kinos move via assign events.

**Plan:** [[kinora-hxmw:plan]]

**Done when:** roots are config-declared with per-root policies (inbox auto-provisioned), `kinora assign` / `store --root` write assign events, `kinora compact` compacts all roots consuming live assigns with `AmbiguousAssign`/`UnknownRoot` fail-loud errors, GC drops aged unpinned entries, and cross-root composition refs prevent the owning root from dropping a referenced version.

## Outcome
Resolved across 7 completed child tasks (c48l, szkl, g08g, l79b, 7mou, mngq, f0rg). Delivered: `roots {}` config block + `RootPolicy` (`never`/`MaxAge`/`KeepLastN`) with inbox auto-provision; the `assign` event with `supersedes` chain resolution; `compact_root`/`compact_all` split with a no-`--root` always-all CLI and per-root error isolation; D2-style `AmbiguousAssign`/`UnknownRoot` rendering with copy-pasteable resolution hints; policy-driven root-entry GC + hot-ledger prune + `pin` propagation; and `ExternalRefs` cross-root integrity surfaced as a retention clause on compact output. 301 lib + 81 CLI tests pass. Satisfied the precondition for per-root rendering — every kino has exactly one owning root post-compact.
