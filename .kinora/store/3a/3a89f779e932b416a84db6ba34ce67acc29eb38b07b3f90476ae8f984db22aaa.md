# Task: config `roots {}` block + inbox auto-provision

**Goal.** Add `RootPolicy` and `config.roots` parsing so downstream compact/GC have a declarative source of truth for named roots; auto-provision the `inbox` default.

**Realizes:**
- [[kinora-xi21:spec-4]] — Multiple named roots, each with its own GC policy; inbox is the default.

**Plan:** [[kinora-hxmw:plan]]

**Done when:** `config.styx` `roots { <name> { policy "<s>" } }` parses into `Config.roots: BTreeMap<String, RootPolicy>`; policy grammar accepts `never`/`<N><unit>`/`keep-last-<N>` and rejects unknown forms with `ConfigError::InvalidPolicy`; `inbox → MaxAge("30d")` auto-provisions when absent; and `kinora init` writes an explicit `roots { inbox { policy "30d" } }` block.

## Outcome
Resolved. `RootPolicy` (`Never`/`MaxAge(String)`/`KeepLastN(usize)`) with case-sensitive grammar; two-layer facet-derived `RawConfig`→validated `Config` parse keeps validation independent of facet_styx mechanics. `to_styx` always emits the block to round-trip. 13 config tests + init tests; workspace green (296). Deferred: styx-layer duplicate root-name detection (BTreeMap collapses dupes silently). Commits `eafa453`, `949776a`, `5aaaf55`.
