# Plan: Clean staged after commit; preserve history as `commits`-root archive

**Task:** kino://241eaa31e6bbd2bcff3857b9c54cc0ca3d0b1a95b010d3171f7304d9496b2292

Three TDD phases, each a commit: config provisioning, then the archive format,
then wiring it into the commit pipeline.

## Phase A — config auto-provision *(done)*
Auto-inject the `commits` root with `RootPolicy::Never` in `Config::from_styx`
when absent, mirroring the existing `inbox` provisioning; user override preserved.

> **Done.** Single-line addition after the inbox auto-provision; tests confirm
> commits is injected when absent, user policy override wins, and config
> serializes back cleanly.

## Phase B — archive serialization format *(done)*
New `kinora::commit_archive` module: `serialize_archive` (header line
`{"@schema":"kinora-commit-archive-v1"}` then one event-json per line) and
`parse_archive` with a hand-rolled header parser.

> **Done.** Round-trip, empty-events, and unknown-schema-rejection tests pass.

## Phase C — wire archive + cleanup + special-case *(done)*
`maybe_archive_owned_events` in `commit_root_with_refs`; `commit_all` iterates
`commits` last; render skips `commits`; `commits` root special-cases self-archive.

> **Done.** Four end-to-end tests lock the invariants (archive + assign produced,
> commits doesn't archive itself, commits kinograph contains the archive, repeated
> runs idempotent). Review follow-up commit tightened malformed-assign propagation,
> documented the two idempotency guards as crash-recovery nets, and added a
> "two runs with new activity stack distinct archives" test. Staged cleanup itself
> deferred to kino://ad0dc4a32969fb27be036acdf5a258b540785ff85a63d7a086fab9bd817863e3.
