# Task: Deliver the kinora bootstrap

**Goal.** Drive the bootstrap epic to completion: land every child feature (data layer, `store`, `resolve`, kinograph, `render`, dogfood) and confirm the end-to-end loop works.

**Realizes:**
- [[kinora-w7w0:spec]] — Kinora bootstrap.

**Done when:** all child features are complete and dogfooding the kudo RFCs through store/render surfaces no blocking issues. Achieved — the bootstrap CLI (`init`, `store`, `resolve`, `render`) ships with a working kinograph composition format; the transitional storage layout is documented as superseded by [[kinora-xi21:spec]].

## Outcome
- Children resolved: [[kinora-fhw1]], [[kinora-5k13:task]], [[kinora-860i:task]], [[kinora-6zxd:task]], [[kinora-zboo:task]], [[kinora-9nom:task]], [[kinora-cium:task]].
- Beans-sync deferred to post-bootstrap.
