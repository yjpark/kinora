# Task: one-file-per-event hot ledger

**Goal.** Swap the ledger's write unit from "append to a per-lineage file" to "create one immutable file per event" under `.kinora/hot/<ab>/<event-hash>.jsonl`, sharded like the blob store and merge-safe across branches.

**Realizes:**
- [[kinora-xi21:spec-1]] — Hot ledger is one immutable file per event.

**Done when:** `store` writes to `hot/` (not `ledger/`), `Ledger::write_event`/`read_all_events` round-trip and dedup by event hash idempotently, readers transparently union legacy `ledger/` + new `hot/` events, and cross-branch merge simulation produces no conflicts.

## Outcome
Resolved. Added `paths::hot_*`, `Event::event_hash()` (full 64-hex BLAKE3 of the canonical JSON line), crash-atomic `write_event` (tmp+rename) and `read_all_events` (shard-scan + dedup via `create_new`). `store_kino` routes through `write_event`; `Resolver::load` unions legacy + hot events. HEAD no longer written by `store_kino` (retained only as a legacy tiebreak hint). 218 workspace tests pass; dogfooded against existing legacy lineage data. Commits `1f99f95`, `dbfa7d9` + review fixes.
