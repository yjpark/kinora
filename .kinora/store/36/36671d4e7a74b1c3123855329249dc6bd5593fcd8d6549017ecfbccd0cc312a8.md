# Task: implement self-contained root kinograph header

**Goal.** Make root kinographs carry their commit metadata in a header line so no `kind: root` staged event is written, and `.kinora/staged/` is purely pending events after commit + repack.

**Realizes:**
- [[kinora-et1t:spec]] — Root kinographs are self-contained: commit metadata lives in a header line.

**Done when:** `init → store_md → commit → repack` leaves `staged/` empty while `roots/` and `store/` are populated; version history walks via `roots/<name>` → blob → header → `parents` with no ledger read; and `kinora render`/`resolve` still work against the new format.

## Outcome
Resolved. `RootHeader { kind: "root", id, parents, ts, author, provenance }` as line 1 of every root blob; `kind` is the parse discriminator. `new_genesis` derives the lineage id from `hash(entries-only bytes)`, `new_child` carries it forward. `commit_root_with_refs` writes blob + pointer directly. Hard cutover: legacy root blobs no longer parse; `reformat.rs` Step 1 and `PriorRootEventMissing` removed; Step 2 (kinograph styx→styxl) stays. CLI reformat summary no longer reports root counts. Tests cover header/lineage/multi-parent roundtrips, lineage-id stability, zero-staged-after-repack, and `WrongHeaderKind`. Follow-up: pre-et1t repos must nuke and rebuild (no migration code). Commits `4e80bf5`, `b64f6cf`, `7e79803`.
