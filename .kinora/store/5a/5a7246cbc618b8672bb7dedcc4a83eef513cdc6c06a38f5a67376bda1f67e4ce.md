# Task: drop legacy ledger layout (remove HEAD/lineage/.kinora/ledger code)

**Goal.** Remove the vestigial pre-mjvb ledger code paths — no production write path touches `.kinora/ledger/`, and resolve only reads it as a backward-compat stub — so the dir stops being created and the resolver shrinks.

**Realizes:**
- [[kinora-xi21:spec-1]] — Hot ledger is one immutable file per event.
- [[kinora-et1t:spec]] — Root kinographs are self-contained (shares the hard-cutover posture).

**Done when:** `rg ledger_dir|head_path|HEAD_FILE|LEDGER_` returns no matches in `crates/kinora/src`; a fresh `kinora init` does not create `.kinora/ledger` or `.kinora/HEAD`; and all tests pass with zero warnings.

## Outcome
Resolved. Removed from `paths.rs` the `HEAD_FILE`/`LEDGER_*` constants and `head_path`/`ledger_dir`/`ledger_file_path`; from `ledger.rs` the `mint_and_append`/`append_to_head`/`read_lineage`/`read_all_lineages`/`current_lineage`/`set_head` methods + `NoHead`/`LineageAlreadyExists`/`LineageMissing` variants (~11 tests); from `resolve.rs` the legacy-lineage loop, `head_for_current_lineage`, and its `pick_head` callsite (multi-head always `MultipleHeads`) + 3 legacy tests; and the legacy mkdir from `init.rs`/`clone.rs`. `ensure_layout` now only creates `staged/`. Hard cutover per the et1t spirit; dogfood repo already rebuilt post-et1t.
