# Task: Dogfood — extend stencil api-kinograph to engine/region/spec/kinds/lib

**Goal.** Extend the kino://4570a3c35dc0d6bd0f0cf2892dcd0a42c8115a62c763a8f8b734658a88406c50 dogfood from `target.rs` to the rest
of stencil's public surface (27 remaining items across engine.rs, region.rs,
spec.rs, kinds.rs, lib.rs), following the se7b convention, until the whole crate
is self-hosting.

**Realizes:**
- kino://5898fe2453ef8824f71670df106512d00502edbc90ac0fa8705a5d03e6f98c25 — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Plan:** kino://ce7828effe91af8b1ad9ce15ce5bdb606156a429b6084787c82c785f1457c304

**Done when:** api-spec kinos exist for all remaining public items; per-module
api-kinographs bind each file; `stencil sync crates/stencil/src` fills every
read-only block; the crate compiles; all tests pass; and a second sync reports 0
changed across all 6 modules (stencil fully self-hosting, including the engine
that performs the sync).

## Outcome
All 6 stencil modules (target, spec, kinds, lib, region, engine — 29 public
items) are stencil-managed from per-module api-kinographs; `stencil sync` over
the whole tree re-runs to 0 changed. **Open question resolved:** kinograph
granularity = per-module/per-file (a crate-wide kinograph would make every other
file report foreign items as unslotted). Convention validated across every Rust
item kind incl. multi-line fn signatures. Content-model findings (both handled,
no stencil change): a definition containing ` ```rust ` → 4-backtick fence;
prose containing inline ` ```rust ` → renders literally.
