# Task: Styxl writer + `kinora reformat` command

**Goal.** Add `to_styxl`/`parse_styxl` for Kinograph and RootKinograph, route
kinograph-kind writes to `.styxl`, and build `kinora reformat` to migrate existing
`.styx`-wrapped blobs to styxl via staged new-version events.

**Realizes:**
- [[kinora-tx3e:spec]] — Kinograph kinos use the styxl one-entry-per-line format.

**Plan:** [[kinora-tx3e:plan]]

**Done when:** kinograph kinos are written one-entry-per-line as `.styxl`;
`kinora reformat` stages styxl versions for legacy `.styx` kinographs and rewrites
root kinographs directly; after `kinora commit` the reformatted kinos are heads and
render output is unchanged; reformat is idempotent on already-styxl repos (zero
staged events); zero warnings.

## Outcome
Completed in 6 commits. `to_styxl`/`parse_styxl`/`is_styxl` added to kinograph.rs and
root.rs; kind→extension routes to `.styxl`. `kinora::reformat::reformat_repo` walks
reachable kinos from each root, restages changed kinograph kinos as new versions,
and rewrites root kinographs through `store_kino(kind:"root", ...)` (keeping
event-graph linkage). Two idempotency guards (`is_styxl` fast path, then
byte-equality after reserialize); UTF-8 guard falls through to parse. CLI summary
suggests `kinora commit` only when versions were staged. 7 library tests + review
fixes (NoHead variant, UTF-8 guard, hoisted ledger read).
