# Task: Second language target (TS or Python)

**Goal.** Add a non-Rust `LanguageTarget` (TypeScript or Python) to prove the
engine needs no redesign for new languages (RFC principle 3), validating the
comment-leader / doc-format / block-emission abstraction.

**Realizes:**
- [[kinora-thow:spec]] — Stencil marker protocol, region model, and LanguageTarget trait.
- [[kinora-bm7z:spec]] — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Done when:** a second `LanguageTarget` implementation renders a spec kino into
that language's read-only blocks (correct comment leader + doc-comment form +
block emission) and re-syncs clean, with no changes required to the engine
itself.
