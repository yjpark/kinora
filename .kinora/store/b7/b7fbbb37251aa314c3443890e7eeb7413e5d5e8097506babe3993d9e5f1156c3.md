# Task: compact library split (compact_root / compact_all) + always-all CLI

**Goal.** Split phase-2B's single `compact(kin_root, name, params)` into a per-root core (`compact_root`) and a batch driver (`compact_all`), and retire the `--root` flag from `kinora compact` (D5). Pure refactor + CLI shape change — no new compaction semantics.

**Realizes:**
- [[kinora-xi21:spec-4]] — Multiple named roots, each with its own GC policy; inbox is the default.

**Plan:** [[kinora-hxmw:plan]]

**Done when:** `compact_root` keeps the old `compact` signature; `compact_all` loads `Config`, iterates `config.roots` in name order, collects per-root results without short-circuiting on a per-root error, and only outer-`Err`s on pre-iteration config failures; `kinora compact` takes no `--root`, prints one line per root, and exits 1 iff any root errored (clean roots still advance).

## Outcome
Resolved. Renamed `compact` → `compact_root`; added `compact_all` returning `Vec<(String, Result<CompactResult, CompactError>)>` plus `CompactError::Config` + `From<ConfigError>`. CLI dropped `root: Option<String>` and `DEFAULT_ROOT_NAME`, always calls `compact_all`, renders `root=<name> ...` / `ERROR: ...` per root. 5 lib + 4 CLI tests (including a figue-level assertion that `--root` is now rejected). 351 tests green; subagent review found no issues. Commit `eaf6901`.
