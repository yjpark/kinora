# Plan: Prune staged events after archive for Never-policy roots

**Task:** kino://ad0dc4a32969fb27be036acdf5a258b540785ff85a63d7a086fab9bd817863e3

Land the rebuild-merge change first so pruning can't orphan entries, then wire the
post-archive prune, then tests.

## Phase 1 — `build_root` signature + prior-root merge *(done)*
Extend `build_root(events, root_name, declared_roots)` →
`(..., prior_root: Option<&RootKinograph>)`. After the staged-events build, merge
prior-root entries whose id is absent AND not reassigned away (preserving
pin/version/note). Feed prior_root only under Never policy.

> **Done.** Merge + `merge_source` Never gating implemented; live-reassign guard
> prevents resurrecting moved kinos; `propagate_pins` retained as the complementary
> path. Unit tests pin pinned-entry preservation, live-reassign respect, and
> no-duplicate behavior.

## Phase 2 — post-archive prune *(done)*
`maybe_archive_owned_events` returns `(archive_id, archived_event_hashes)`. In the
Never branch: non-commits roots drop the archived hashes; the commits root drops
all owned staged events (archive stores + archive-assigns). MaxAge/KeepLastN keep
using `prune_staged_events`.

> **Done.** `drop_staged_events` helper added (NotFound-tolerant for crash replay);
> Never-prune wired for both branches. Dropping all owned commits-root events also
> closes an inbox-routing trap where an orphaned archive store would default-route.

## Phase 3 — tests + regression *(done)*
End-to-end drains for both branches, merge-no-activity, merge-respects-reassign,
3 `build_root` units; adapt placeholder-Never tests and archive-counting tests.

> **Done.** 7 new + 8 adapted tests; 498 workspace tests pass, zero warnings,
> subagent review clean.
