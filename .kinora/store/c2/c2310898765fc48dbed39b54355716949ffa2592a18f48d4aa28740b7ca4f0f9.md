# Plan: Phase 3 — multiple named roots + assign + per-root GC

**Task:** [[kinora-hxmw:task]]

Seven shift-sized children with dependency-enforced ordering; config (#1) and event-schema (#2) run first in parallel, then the assign path (#3→#4→#5), then GC (#6) and integrity (#7).

## Phase 1 — Config `roots {}` block + inbox auto-provision *(done)*
`RootPolicy` enum, `roots {}` parsing, inbox default. See [[kinora-c48l:task]].

> **Done.** `RootPolicy` (`Never`/`MaxAge`/`KeepLastN`), two-layer raw→domain parse, inbox auto-provisions to `MaxAge("30d")`, `kinora init` writes a visible block. 13 config tests; workspace green (296).

## Phase 2 — Event schema generalization *(done)*
Add an event-kind discriminator distinct from content kind. See [[kinora-szkl:task]].

> **Done.** Flat `event_kind: String` (first field), `EVENT_KIND_STORE`, `is_store_event()`, narrow legacy fallback. Consumers (resolver, compact) filter on store events. 303 tests green.

## Phase 3 — Assign event + CLI + `store --root` atomic pair *(done)*
`AssignEvent`, `kinora assign`, birth-assign pair. See [[kinora-g08g:task]].

> **Done.** `AssignEvent` round-trips via the shared `Event` serializer (`event_kind = "assign"`, `parents = supersedes`); `kinora assign <kino> <root> --resolves`; `store --root` writes a rollback-safe store+assign pair. 24 lib + 16 CLI tests.

## Phase 4 — Compact library split + always-all CLI *(done)*
`compact` → `compact_root` + `compact_all`; retire `--root` (D5). See [[kinora-l79b:task]].

> **Done.** Per-root core + batch driver iterating `config.roots` in name order; per-root errors don't short-circuit; CLI prints one line per root, exit 1 iff any errored. 351 tests green.

## Phase 5 — Compact consumes assigns + AmbiguousAssign/UnknownRoot *(done)*
Live-assign graph, fail-loud errors, inbox-default routing (D2/D4). See [[kinora-7mou:task]].

> **Done.** `collect_live_assigns` (transitive supersedes), `kino_target_root` routing, `build_root` routes before `pick_head`, D2 CLI rendering. Bulk test migration to compact-to-inbox. 287 lib + 77 CLI tests.

## Phase 6 — GC/prune per policy + pin support *(done)*
Policy-driven entry GC + hot-ledger prune + pin propagation. See [[kinora-mngq:task]].

> **Done.** `RootPolicy::max_age_seconds()` drives `apply_root_entry_gc` (MaxAge), `prune_hot_events` (MaxAge + KeepLastN), and `propagate_pins` (pin wins over head; ownership wins over pin). 9 new tests; 296 lib + 77 CLI green.

## Phase 7 — Cross-root integrity *(done)*
External composition refs prevent the owning root's GC drop. See [[kinora-f0rg:task]].

> **Done.** `ExternalRefs::collect` snapshots every other root's composition pointers once per batch; `(id, version)` pairs treated as implicit pins for GC + hot-prune; `CompactResult.retained_by_cross_root` rendered as a trailing CLI clause. Circular refs handled (flat walk). 301 lib + 81 CLI tests.
