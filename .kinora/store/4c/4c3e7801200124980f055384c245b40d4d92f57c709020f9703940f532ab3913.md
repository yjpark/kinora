# Plan: `kinora clone` — rebuild `.kinora/` into a fresh directory

**Task:** [[kinora-b1mg:task]]

Library module first (mirrors `reformat.rs` structure), then the CLI wrapper, then
review fixes.

## Phase A — library `kinora::clone` module *(done)*
`clone_repo(src, dst, CloneParams) -> CloneReport`: validate dst empty, init dst
layout, walk reachable blobs from root pointers (recursing into kinograph-kind
entries' heads), re-store each via `dst_store.write`, copy root pointers, copy
ledger events for reachable blobs, report counts.

> **Done.** Implemented with hash verification inherited from `ContentStore::read`.
> Pragmatic deviation from bean step 1: the staged-empty precondition was dropped
> because pre-bayr every repo has populated staged; instead events are filtered
> during copy (store events with reachable hash kept, assigns kept verbatim). To be
> re-instated naturally once [[kinora-wcpp:task]] drains staged. Review fix reused
> `Ledger::write_event` and covered the all-reachable case.

## Phase B — CLI command `kinora clone <src> <dst>` *(done)*
Add a `Clone { src, dst }` command variant; new `kinora-cli/src/clone.rs` taking
both paths verbatim (no find_repo_root walk-up); reject `-C`/`--repo-root`; format
the summary.

> **Done.** Thin CLI shell; 9 tests cover success, src-not-kinora / dst-not-empty
> errors, relative-path resolution, default author/provenance, and summary
> formatting at 0/1/many counts.

## Phase C — review fixes *(done)*
> **Done.** Folded into the Phase A and Phase B commits (Phase A review fixes in
> `dac331d`, Phase B+C in `67f1951`).
