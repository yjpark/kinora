# Task: CLI `store` command

**Goal.** Wire `kinora store` end-to-end on the data layer: read content (stdin or file), hash with BLAKE3, dedupe into `.kinora/store/`, build and validate the event envelope, and mint-or-append to the current lineage — leaving the workspace ready for `git add` + `git commit`.

**Realizes:**
- [[kinora-5k13:task]] — data layer it sits on.
- [[kinora-fhw1:spec-3]] — birth vs version events, the kino model.
- [[kinora-fhw1:spec-5]] — library-first orchestration, thin CLI wrapper.

**Done when:** `kinora store <kind> [path]` accepts stdin and file content; records name/kind/provenance/draft/timestamp (provenance required); dedupes blob writes by hash while still appending a ledger event; validates namespace rules and parent existence; mints a new lineage when HEAD is absent and appends otherwise, updating `.kinora/HEAD`. Verified by library + CLI tests.

## Outcome
- **Library** — `author::resolve_author_from_git()` reads `user.name` from the repo's merged git config via gix (None for non-git/unset). `kino::store_kino()` orchestrates write-blob (dedup) → birth-vs-version decision (`--parents` without `--id` rejected as `ParentsWithoutId`) → build envelope → `validate_event_shape` + `validate_parents_exist` → mint-or-append by HEAD presence.
- **CLI** — figue-derived `Cli`/`Command::Store` (positional `kind`+`path`, required `--provenance`, optional `--name`/`--id`/`--parents`/`--author`, `--draft`, repeatable `-m key=value`; flattens `FigueBuiltins` for `--help`/`--version`/`--completions`). `common.rs` walks up to find `.kinora/`, parses `KEY=VALUE` (values may contain `=`), splits comma parents. `store.rs` reads file/stdin, assembles metadata, resolves author (flag > git), stamps via jiff; rejects `--draft` + `-m draft=X` collisions (`ConflictingDraftFlag`). `main.rs` exits 0 for help/version/completions, 1 for failures; prints `stored kind=… id=… hash=… lineage=…`.
- **MVP lineage model** documented inline: one lineage per repo (HEAD-absent mints, present appends); per-branch/per-identity partitioning deferred — identity lives in `event.id`, not the lineage filename.

113 workspace tests (94 lib + 19 CLI), zero warnings; smoke-tested end-to-end against a hand-laid `.kinora/`.

## Follow-ups
- Wire `kinora init` into the CLI (library `init()` already complete).
- Branch-aware lineage minting (`git rev-parse`).
- Warn on stray empty `--parents` entries (`,,abc`).
