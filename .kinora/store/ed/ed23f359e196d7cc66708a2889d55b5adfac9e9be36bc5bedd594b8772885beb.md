# Plan: Multi-branch + worktree render enumeration

**Task:** kino://23060387e21aae5f7d098222337bc69925e7d37685acdeb6f267593aa0237d83

Library-first, test-driven: build the git-state reader, wire it into render, then
cover the edge cases.

## Phase 1 — `git_state` library module *(done)*
New `crates/kinora/src/git_state.rs` with `extract_subtree` (walk a commit's tree,
write each `.kinora/` blob to a dst dir, skip symlinks/gitlinks, error if absent),
`list_local_branches` (peel `refs/heads/*` to commit ids), and `list_worktrees`
(enumerate linked worktrees, main excluded).

> **Done.** Module ships on gix 0.81 reading committed state with no working-copy
> dependency; 8 git_state tests including SubtreeAbsent and detached-HEAD worktree.

## Phase 2 — Wire git_state into multi-source render *(done)*
Change CLI render from single-resolver to multi-source: open the repo, enumerate
branches + worktrees, dedupe by ref name, extract `.kinora/` per source to a tempdir,
build a Resolver per source, render with branch-name as the group label, and merge
all per-source Books before `write_book`. Fall back to the working copy with legacy
grouping when no branch has committed `.kinora/`.

> **Done.** One top-level group per source; per-branch pages with cited source
> markers; `RenderReport.source_count` surfaced on the CLI success line. 11
> render-focused tests; 489 workspace tests pass, zero warnings.

## Phase 3 — Review-driven edge coverage *(done)*
Cover SubtreeAbsent-per-branch (pre-kinora branches skipped, not aborting) and
detached-HEAD worktree labelling.

> **Done.** Commit 1a3360f added the review-driven tests; deferred items
> (cross-branch `kino://` resolution, gix walk-up) recorded as follow-ups on the
> task.
