# Task: CLI — stencil scaffold <kinograph>

**Goal.** Implement `stencil scaffold <kinograph>`: given an api-kinograph (by
name or id), generate a new source file — `stencil:kinograph` header plus one
`stencil:slot` + filled read-only block per entry in kinograph order — by
reusing the engine + RustTarget. TDD/integration.

**Realizes:**
- kino://5898fe2453ef8824f71670df106512d00502edbc90ac0fa8705a5d03e6f98c25 — Stencil: a kinora-native, language-agnostic crate-API preprocessor.

**Done when:** `run_scaffold` builds a skeleton (binding header + one
blank-line-separated `stencil:slot` per entry in kinograph order), parses it,
runs `sync_file` to fill the blocks, and prints to stdout; the engine exposes
`kinograph_slot_names` (ordered, tolerant, fail-loud on dup names); scaffold
output re-syncs clean; unknown/non-kinograph references error; all tests pass
with zero warnings.

## Outcome
Implemented `crates/stencil-cli/src/scaffold.rs`: `run_scaffold` → stdout
(composable, no clobber). Engine refactor: factored `build_index` into a shared
`resolve_entries` preserving document order, added `kinograph_slot_names`.
`main.rs` wires `Command::Scaffold`; removed the stale `NotImplemented` variant.
11 tests (6 scaffold + 5 engine). Review fix: `StencilError::UnslottableEntryName`
for empty/whitespace names so scaffold never emits an unparseable slot marker.
Full workspace green, zero warnings, clippy clean.
