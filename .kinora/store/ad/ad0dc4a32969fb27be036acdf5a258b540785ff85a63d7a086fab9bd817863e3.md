# Task: Prune staged events after archive for Never-policy roots

**Goal.** Drop owned staged events after a successful archive for `Never`-policy
roots, and add the `prior_root` merge in `build_root` so rebuilds still see kinos
that were archived-and-pruned across subsequent commits.

**Realizes:**
- [[kinora-bayr:spec]] — Prune staged events after archive, rebuild via prior-root merge.
- [[kinora-q6bo:spec]] — Commit archives staged events into a per-root `commit-archive` kino.

**Plan:** [[kinora-bayr:plan]]

**Done when:** committing a Never root drains its owned staged events (archive
blob exists, root kinograph keeps the entries); the commits root drains its
consumed archive stores + assigns; rebuild with no new activity preserves prior
entries; a live reassign removes the entry on next rebuild; non-Never policies are
unchanged; zero warnings; full suite green.

## Outcome
Shipped in 3 commits (498 tests, clean review). `build_root` now merges prior-root
entries (id-missing + no live reassign) under Never policy only;
`maybe_archive_owned_events` returns the archived hashes; new `drop_staged_events`
helper; Never-prune wired for both non-commits and commits roots. 7 tests added,
8 adapted (Never→keep-last-10 placeholders switched; archive-counting tests moved
to inspect the durable commits kinograph instead of staging). Surfaced a Resolver
gap closed by [[kinora-bwo8:task]].
