# Task: Drain archived events from staging for MaxAge (generalize bayr)

**Goal.** Extend bayr's post-archive drain + `prior_root` merge from
`RootPolicy::Never` alone to `Never | MaxAge(_)`, so MaxAge roots (including the
default `inbox`, MaxAge 30d) stop accumulating committed events in staging.

**Realizes:**
- [[kinora-bayr:spec]] — Prune staged events after archive, rebuild via prior-root merge.
- [[kinora-q6bo:spec]] — Commit archives staged events into a per-root `commit-archive` kino.

**Done when:** committing a MaxAge root drains its archived events from staging;
entries age out via root-kinograph GC (`apply_root_entry_gc`), not staging;
prior_root merges entries across MaxAge commits; Never and KeepLastN regressions
stay green; `kinora commit` + `kinora repack` on a default inbox root leaves
staged empty; zero warnings.

## Outcome
Shipped in 3 commits (512 tests). The `build_root` merge_source gate and both
post-archive drain gates (non-commits + commits) widened to `Never | MaxAge(_)`.
`prune_staged_events` now early-returns for `Never | MaxAge(_)` — the MaxAge branch
(~80 lines), the `now` param, and `owned_assigns` collection were removed since
retention now lives on the root kinograph via `apply_root_entry_gc` (using per-entry
`head_ts`). `KeepLastN` intentionally untouched (its "N versions per kino" semantic
is load-bearing on staging). Also patched `reformat_repo` Step 2 to synthesize
store-event stubs from root kinograph entries so `pick_head` resolves archived
heads. 3 tests added, 5 adapted. Left a nested-composition reformat gap closed by
[[kinora-ou1u:task]], and a migration-debt repack gap closed by [[kinora-ojc8:task]].
